#include <stdio.h>
#include "main.h"

/**
 * mul - entry point
 * to multiply two interger
 *@a: first interger
 *@b: first interger
 * Return: int
 */

int mul(int a, int b)
{
	int total = a * b;

	return (total);
}
