#include <stdio.h>

/**
 * main - printing the name of a string
 * @argc: the argument value count
 * @argv: the array value
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc * 0]);
	return (0);
}
