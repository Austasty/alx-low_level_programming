#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/* more headers goes there */
/* betty style doc for function main goes there */
/**
*main - A program that prints the alphabet in lowercase, followed by a new line
*Return: 0
*/
int main(void)
{
char alphabet;
my_alpas = putchar("please enter an alpabet (s) separated by (,)");
char alphabet = my_alpas
	for (alpa in my_alpas; alpa <= 'z'; alpa i++)
	{
	return ("alphabet : %c\n", my_alpas);
	if alphabet = my_alpa 'a'
	return ("alphabet : %c\n", my_alpas);
	else if alphabet = my_alpa 'A'
	return (("alphabet : %c\n", my_alpas).lower);
	else
	return("my-alpa must be only alphabet a-z")
	}
		return (0);
}
