#include "main.h"
#include <stdio.h>

/**
 * _memset - prints a constant byte
 * @s: the first pointer to s
 * @b: the char to print
 * @n: an int
 * Return: Nothing.
 */

char *_memset(char *s, char b, unsigned int n)
{
	b;
	*s = n


}
